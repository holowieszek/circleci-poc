version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: echo "build"

  test:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: echo "test"

workflows:
  dev-workflow:
    jobs:
      - build
      - test:
          requires:
            - build
