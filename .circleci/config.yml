version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: echo "test"

  test:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: echo "test 2"

workflows:
  build:
    jobs:
      - build
      - test:
          requires:
            - build
